# 0.2.92 真实宿主验收延期发布例外

## 决定与边界

用户于 2026-09-23 指示：完成真机验收之外的发版工作后发布 0.2.92。该决定改变了原本“21 个 P0 分发、5 个 Custom 目标及 0.2.89/0.2.91 升级全部真机通过后才能发布”的前置门槛。本例外只适用于 0.2.92，**不代表任何真实宿主验收已完成**；发布说明必须明确披露该限制。后续版本仍使用原有完整验收链路。

## 发布物身份

使用已通过 GitHub 候选 workflow 35841255325 的原始 arm64 签名、公证并装订的候选制品，不重新构建或重签 App、DMG 或 updater ZIP。其私有源码提交为 `e57c7589c2b77d9fa3a8cab0cc47e4f4a1b33447`，候选公开提交为 `18dbab8f7dbd144f052571f5d20ab44438dfab77`。仓库内 `release-evidence/deferred-host-acceptance/0.2.92.json` 冻结上传回执 artifact、候选 artifact、GitHub 上传摘要、物理 App、DMG 和 ZIP 摘要，并显式记录 `not_performed`。

tag workflow 在 Apple Silicon runner 上下载精确 artifact 和上传回执，先比较外层 SHA-256、精确文件清单、内层 `files.sha256` 与候选验证回执，再重算 App 物理树、验证 Developer ID、Gatekeeper 和 staple。DMG 与 updater ZIP 原字节进入 draft Release；仅对既有 updater ZIP 按锁定的 electron-builder 算法补出候选 artifact 未收录的 `.zip.blockmap`。`latest-mac.yml` 从候选更新元数据合并并重新核对容器大小和 SHA-512。预发布 workflow_dispatch 先走同一验证路径，不创建 tag 或 Release；tag push 通过后才创建 draft。

## 仍然执行的门槛

私有 main 的精确 CI、Mac Studio health、公开 main CI、源码与版本绑定、发行清单校验、候选性能阈值、Apple 签名/公证、容器校验、打包 Worker/CLI smoke 均已通过。例外提交本身须通过公开 CI 和无发布的预发布 workflow。发布网站、Ed25519 更新签名、完整 asset 列表、云端健康检查、0.2.89 与 0.2.91 客户端更新 API 验证仍按原顺序完成；任一失败停在 draft，不发布。

## 刻意不做的事

不生成、伪造或修改 `real_host` evidence，不把静态 receipt、候选 CI 或启动探针表述为 21 个 P0 分发及 5 个 Custom 已通过。正式发布说明逐宿主披露连接能力，并写明真实宿主、生命周期、Custom 和升级验证延期。例外不是常规 `release.mjs` 路径；常规路径继续要求完整验收，未来同步不会自动继承这个一次性例外。
